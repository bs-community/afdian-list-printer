<table>
  <tbody>
    {% for chunk in users -%}
    <tr>
      {% for user in chunk -%}
      <td align=center>
        <a href="{{ user.url_slug }}">
          <img src="{{ user.avatar }}" width="{{ avatar_size }}" height="{{ avatar_size }}">
          <br>
          {{ user.name }}
        </a>
      </td>
      {% endfor -%}
    </tr>
    {% endfor -%}
  </tbody>
</table>
